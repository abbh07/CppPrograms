       #include<iostream.h>
       #include<conio.h>
       void main()
       (
       float x,y,sum;
       clrscr();
       cout<<"Enter the value of first number ";
       cin>>x;
       cout<<"Enter the value of second number ";
       cin>>y;
       sum=x+y;
       cout<<"The sum is "<<sum;
       getch();
       }
